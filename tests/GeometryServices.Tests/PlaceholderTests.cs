using Xunit;

namespace MCPAccelerator.Tests.GeometryServices
{
    /// <summary>
    /// Placeholder so xUnit v3 has at least one test to discover and can launch
    /// a test host. Remove once real GeometryServices tests are added.
    /// </summary>
    public class PlaceholderTests
    {
        [Fact]
        public void Placeholder_AlwaysPasses()
        {
            Assert.True(true);
        }
    }
}
